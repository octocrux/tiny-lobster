{
  "name": "tiny-lobster",
  "version": "0.1.0",
  "description": "My teensy-tinsy node.js pet",
  "main": "index.js",
  "repository": "git@github.com:octocrux/tiny-lobster.git",
  "author": "octocrux <cate.petrusevich@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist/* && mkdir -p dist",
    "start": "yarn build && PORT=3001 NODE_ENV=production node dist/app.js",
    "build": "yarn clean && tsc --noEmit false --outDir dist",
    "dev": "PORT=3000 NODE_ENV=dev nodemon --config nodemon.json src/app.ts",
    "dev:debug": "PORT=3000 NODE_ENV=dev nodemon --config nodemon.json --inspect src/app.ts",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "express": "^4.17.1",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.10.1",
    "@typescript-eslint/eslint-plugin": "^4.31.2",
    "@typescript-eslint/parser": "^4.31.2",
    "eslint": "^7.32.0",
    "eslint-plugin-simple-import-sort": "^7.0.0",
    "nodemon": "^2.0.13",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}
